{
  "name": "rcp-data-imob",
  "version": "1.0.0",
  "private": true,
  "description": "Orquestração de desenvolvimento: sobe db + backend + frontend (Docker) e o app mobile (Expo) com um comando.",
  "scripts": {
    "dev": "node scripts/dev-setup.js && docker compose up --build -d && cd mobile && npm install && npm start",
    "up": "docker compose up --build -d",
    "mobile": "node scripts/dev-setup.js && cd mobile && npm install && npm start",
    "logs": "docker compose logs -f",
    "stop": "docker compose down",
    "reset": "docker compose down -v"
  }
}
